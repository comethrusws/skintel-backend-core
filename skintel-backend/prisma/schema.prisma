generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider  = "postgresql"
  url       = env("DATABASE_URL")
  directUrl = env("DATABASE_URL")
}

model AnonymousSession {
  id                String             @id @default(cuid())
  sessionId         String             @unique @map("session_id")
  sessionToken      String             @unique @map("session_token")
  deviceId          String             @map("device_id")
  deviceInfo        Json               @map("device_info")
  createdAt         DateTime           @default(now()) @map("created_at")
  expiresAt         DateTime           @map("expires_at")
  mergedToUserId    String?            @map("merged_to_user_id")
  answers           OnboardingAnswer[]
  onboardingSession OnboardingSession?

  @@map("anonymous_sessions")
}

model User {
  id                      String                  @id @default(cuid())
  userId                  String                  @unique @map("user_id")
  email                   String?                 @unique
  passwordHash            String?                 @map("password_hash")
  ssoProvider             String?                 @map("sso_provider")
  ssoId                   String?                 @map("sso_id")
  createdAt               DateTime                @default(now()) @map("created_at")
  updatedAt               DateTime                @updatedAt @map("updated_at")
  dateOfBirth             DateTime?               @map("date_of_birth")
  name                    String?
  phoneNumber             String?                 @map("phone_number")
  planType                PlanType                @default(MONTHLY) @map("plan_type")
  latitude                Float?                  @map("latitude")
  locationUpdatedAt       DateTime?               @map("location_updated_at")
  longitude               Float?                  @map("longitude")
  originalTransactionId   String?                 @map("original_transaction_id")
  subscriptionExpiresAt   DateTime?               @map("subscription_expires_at")
  isActive                Boolean                 @default(false) @map("is_active")
  autoRenewEnabled        Boolean                 @default(true) @map("auto_renew_enabled")
  cancellationDate        DateTime?               @map("cancellation_date")
  lastAppleNotification   DateTime?               @map("last_apple_notification")
  hasConsented            Boolean                 @default(false) @map("has_consented")
  deletedAt               DateTime?               @map("deleted_at")
  Task                    Task[]
  TaskCompletion          TaskCompletion[]
  deviceTokens            DeviceToken[]
  facialLandmarks         FacialLandmarks[]
  notificationPreferences NotificationPreference?
  answers                 OnboardingAnswer[]
  onboardingSession       OnboardingSession?
  passwordResetTokens     PasswordResetToken[]
  products                Product[]
  refreshTokens           RefreshToken[]
  skinFeels               SkinFeel[]
  transactions            Transaction[]
  waterIntakeLogs         WaterIntakeLog[]

  @@unique([ssoProvider, ssoId])
  @@map("users")
}

model NotificationPreference {
  id                        String   @id @default(cuid())
  userId                    String   @unique @map("user_id")
  notificationSound         Boolean  @default(true) @map("notification_sound")
  dailyRoutineReminders     Boolean  @default(false) @map("daily_routine_reminders")
  hydrationAlerts           Boolean  @default(true) @map("hydration_alerts")
  uvIndexAlerts             Boolean  @default(false) @map("uv_index_alerts")
  tipOfTheDay               Boolean  @default(false) @map("tip_of_the_day")
  questionsOfTheDay         Boolean  @default(true) @map("questions_of_the_day")
  smartInsights             Boolean  @default(false) @map("smart_insights")
  ingredientRecommendations Boolean  @default(true) @map("ingredient_recommendations")
  createdAt                 DateTime @default(now()) @map("created_at")
  updatedAt                 DateTime @updatedAt @map("updated_at")
  user                      User     @relation(fields: [userId], references: [userId], onDelete: Cascade)

  @@map("notification_preferences")
}

model DeviceToken {
  id        String   @id @default(cuid())
  userId    String   @map("user_id")
  token     String   @unique
  platform  String   @default("unknown")
  createdAt DateTime @default(now()) @map("created_at")
  updatedAt DateTime @updatedAt @map("updated_at")
  user      User     @relation(fields: [userId], references: [userId], onDelete: Cascade)

  @@map("device_tokens")
}

model RefreshToken {
  id        String   @id @default(cuid())
  token     String   @unique
  userId    String   @map("user_id")
  createdAt DateTime @default(now()) @map("created_at")
  expiresAt DateTime @map("expires_at")
  user      User     @relation(fields: [userId], references: [userId], onDelete: Cascade)

  @@map("refresh_tokens")
}

model OnboardingAnswer {
  id              String            @id @default(cuid())
  answerId        String            @unique @map("answer_id")
  sessionId       String?           @map("session_id")
  userId          String?           @map("user_id")
  screenId        String            @map("screen_id")
  questionId      String            @map("question_id")
  type            AnswerType
  value           Json
  status          AnswerStatus
  savedAt         DateTime          @default(now()) @map("saved_at")
  FacialLandmarks FacialLandmarks?
  session         AnonymousSession? @relation(fields: [sessionId], references: [sessionId], onDelete: Cascade)
  user            User?             @relation(fields: [userId], references: [userId], onDelete: Cascade)

  @@map("onboarding_answers")
}

model OnboardingSession {
  id         String            @id @default(cuid())
  sessionId  String?           @unique @map("session_id")
  userId     String?           @unique @map("user_id")
  allAnswers Json              @map("all_answers")
  status     String            @default("in_progress")
  createdAt  DateTime          @default(now()) @map("created_at")
  updatedAt  DateTime          @updatedAt @map("updated_at")
  session    AnonymousSession? @relation(fields: [sessionId], references: [sessionId], onDelete: Cascade)
  user       User?             @relation(fields: [userId], references: [userId], onDelete: Cascade)

  @@map("onboarding_sessions")
}

model IdempotencyKey {
  id        String   @id @default(cuid())
  key       String   @unique
  response  Json
  createdAt DateTime @default(now()) @map("created_at")
  expiresAt DateTime @map("expires_at")

  @@map("idempotency_keys")
}

model FacialLandmarks {
  id                    String           @id @default(cuid())
  answerId              String           @unique @map("answer_id")
  userId                String?          @map("user_id")
  landmarks             Json
  analysis              Json?
  status                LandmarkStatus   @default(PROCESSING)
  processedAt           DateTime?        @map("processed_at")
  error                 String?
  createdAt             DateTime         @default(now()) @map("created_at")
  analysisType          AnalysisType?
  planEndDate           DateTime?
  planStartDate         DateTime?
  score                 Int?
  weeklyPlan            Json?
  progressUpdate        Json?            @map("progress_update")
  waterIntakeSuggestion Json?            @map("water_intake_suggestion")
  annotatedImageUrl     String?          @map("annotated_image_url")
  answer                OnboardingAnswer @relation(fields: [answerId], references: [answerId], onDelete: Cascade)
  user                  User?            @relation(fields: [userId], references: [userId], onDelete: Cascade)

  @@map("facial_landmarks")
}

model WaterIntakeLog {
  id        String   @id @default(cuid())
  userId    String   @map("user_id")
  date      DateTime @map("date")
  amountMl  Int      @map("amount_ml")
  createdAt DateTime @default(now()) @map("created_at")
  updatedAt DateTime @updatedAt @map("updated_at")
  user      User     @relation(fields: [userId], references: [userId], onDelete: Cascade)

  @@unique([userId, date])
  @@map("water_intake_logs")
}

model Product {
  id          String   @id @default(cuid())
  userId      String   @map("user_id")
  imageUrl    String   @map("image_url")
  productData Json     @map("product_data")
  createdAt   DateTime @default(now()) @map("created_at")
  updatedAt   DateTime @updatedAt @map("updated_at")
  user        User     @relation(fields: [userId], references: [userId], onDelete: Cascade)

  @@map("products")
}

model PasswordResetToken {
  token     String   @id
  userId    String
  expiresAt DateTime
  createdAt DateTime @default(now())
  user      User     @relation(fields: [userId], references: [userId], onDelete: Cascade)

  @@map("password_reset_tokens")
}

model Task {
  id                  String           @id @default(cuid())
  userId              String
  week                Int
  title               String
  description         String
  timeOfDay           String
  recommendedProducts Json?
  userProducts        Json?
  isActive            Boolean          @default(true)
  createdAt           DateTime         @default(now())
  updatedAt           DateTime         @updatedAt
  adaptations         Json?
  category            String
  priority            String
  user                User             @relation(fields: [userId], references: [userId], onDelete: Cascade)
  completions         TaskCompletion[]

  @@index([userId, week])
  @@index([userId, isActive])
  @@index([userId, priority])
}

model TaskCompletion {
  id          String   @id @default(cuid())
  taskId      String
  userId      String
  completedAt DateTime
  timestamp   DateTime @default(now())
  task        Task     @relation(fields: [taskId], references: [id], onDelete: Cascade)
  user        User     @relation(fields: [userId], references: [userId], onDelete: Cascade)

  @@unique([taskId, userId, completedAt])
  @@index([userId, completedAt])
}

model SkinTip {
  id        String   @id @default(cuid())
  date      DateTime @unique @db.Date
  content   String
  category  String?
  createdAt DateTime @default(now()) @map("created_at")

  @@map("skin_tips")
}

model QuestionOfTheDay {
  id        String   @id @default(cuid())
  date      DateTime @unique @db.Date
  question  String
  options   Json?
  category  String?
  createdAt DateTime @default(now()) @map("created_at")

  @@map("questions_of_the_day")
}

model CheckInQuestion {
  id        String   @id @default(cuid())
  date      DateTime @unique @db.Date
  question  String
  createdAt DateTime @default(now()) @map("created_at")

  @@map("check_in_questions")
}

model SkinFeel {
  id        String        @id @default(cuid())
  userId    String        @map("user_id")
  value     SkinFeelValue
  createdAt DateTime      @default(now()) @map("created_at")
  user      User          @relation(fields: [userId], references: [userId], onDelete: Cascade)

  @@map("skin_feels")
}

model RoutineMessage {
  id        String   @id @default(cuid())
  date      DateTime @db.Date
  timeSlot  String   @map("time_slot")
  title     String
  body      String
  createdAt DateTime @default(now()) @map("created_at")

  @@unique([date, timeSlot])
  @@map("routine_messages")
}

model Transaction {
  id                    String    @id @default(cuid())
  userId                String    @map("user_id")
  transactionId         String    @unique @map("transaction_id")
  originalTransactionId String    @map("original_transaction_id")
  productId             String    @map("product_id")
  planType              PlanType  @map("plan_type")
  purchaseDate          DateTime  @map("purchase_date")
  expiresDate           DateTime? @map("expires_date")
  environment           String    @map("environment")
  jwsRepresentation     String?   @map("jws_representation")
  createdAt             DateTime  @default(now()) @map("created_at")
  user                  User      @relation(fields: [userId], references: [userId], onDelete: Cascade)

  @@index([userId])
  @@index([originalTransactionId])
  @@map("transactions")
}

enum AnswerType {
  single
  multi
  slider
  image
  boolean
  derived
}

enum AnswerStatus {
  answered
  skipped
}

enum LandmarkStatus {
  PROCESSING
  COMPLETED
  FAILED
}

enum AnalysisType {
  INITIAL
  PROGRESS
}

enum PlanType {
  WEEKLY
  MONTHLY
}

enum SkinFeelValue {
  feeling_rough
  not_great
  feeling_good
  feeling_fresh
  glowing
}
